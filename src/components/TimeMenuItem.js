import React from 'react'

const TimeMenuItem = ({ content }) => {
  return (
    <a 
        onClick={() => {}} 
        class="block px-4 py-2 text-lg hover:bg-gray-800 hover:cursor-pointer font-medium"
    >{content}</a>
  )
}

export default TimeMenuItem